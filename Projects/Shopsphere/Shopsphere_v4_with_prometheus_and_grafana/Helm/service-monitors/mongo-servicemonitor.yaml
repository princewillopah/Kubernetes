apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mongodb
  namespace: observability
  # labels:
  #   Release: monitoring
spec:
  selector:
    matchLabels:
      app: mongo  #mongodb-exporter
  namespaceSelector:
    matchNames:
      - shopsphere
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s