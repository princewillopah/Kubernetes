apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: luxe-backend
  labels:
    app: api-gateway-service
    tier: gateway
spec:
  selector:
    app: api-gateway-service
  ports:
    - name: metrics
      port: 3000
      targetPort: 3000
  type: ClusterIP