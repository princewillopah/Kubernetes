apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: luxe-backend
  labels:
      app: analytics-service
      tier: business-logic
spec:
  selector:
    app: analytics-service
  ports:
    - name: metrics
      port: 3013
      targetPort: 3013
  type: ClusterIP