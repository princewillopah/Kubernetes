apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: luxe-backend
  labels:
    app: user-service
    tier: business-logic
spec:
  selector:
    app: user-service
  ports:
    - name: metrics
      port: 3002
      targetPort: 3002
  type: ClusterIP