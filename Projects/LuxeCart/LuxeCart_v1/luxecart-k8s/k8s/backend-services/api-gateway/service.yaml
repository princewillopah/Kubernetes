apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: luxe-backend
spec:
  selector:
    app: api-gateway-service
  ports:
  - port: 3000
    targetPort: 3000
  type: ClusterIP