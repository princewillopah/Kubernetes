apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: frontend-nginx
  namespace: observability
spec:
  selector:
    matchLabels:
      app: frontend-nginx
  namespaceSelector:
    matchNames:
      - xxx2
  endpoints:
    - port: metrics
      interval: 15s
